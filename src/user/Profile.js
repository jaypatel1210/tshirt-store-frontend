import React from 'react';
import Base from '../core/Base';

const Profile = () => {
  return (
    <Base title="Profile Dashboard">
      <h2>this is a Profile Dashboard</h2>
    </Base>
  );
};

export default Profile;
