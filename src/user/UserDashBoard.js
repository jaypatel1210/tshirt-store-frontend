import React from 'react';
import Base from '../core/Base';

const UserDashBoard = () => {
  return (
    <Base title="User Dashboard">
      <h2>this is a User Dashboard</h2>
    </Base>
  );
};

export default UserDashBoard;
